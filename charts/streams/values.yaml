image: ghcr.io/fraym/streams
version: 6.3.0
replicas: 2

resources:
  requests:
    cpu: 100m
    memory: 100Mi
  limits:
    memory: 500Mi

autoscaling:
  enabled: true
  minReplicas:
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80

global:
  debug: false
  github:
    username:
    token:
  auth:
    secret:
  postgres:
    connection:

github:
  username:
  token:

auth:
  secret:

postgres:
  connection:
