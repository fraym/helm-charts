image: ghcr.io/fraym/streams
version: 6.3.0
replicas: 2
debug: false

resources:
  requests:
    cpu: 100m
    memory: 100Mi
  limits:
    memory: 500Mi

autoscaling:
  enabled: true
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
