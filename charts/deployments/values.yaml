image: ghcr.io/fraym/deployments
version: 0.28.0
debug: false

resources:
  requests:
    cpu: 10m
    memory: 50Mi
  limits:
    memory: 500Mi

secretName:
  postgres: fraeym-postgres-config
  deployments: fraeym-deployments-config
