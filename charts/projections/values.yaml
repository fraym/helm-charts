image: ghcr.io/fraym/projections
version: 0.35.3
replicas: 2

resources:
  requests:
    cpu: 300m
    memory: 150Mi
  limits:
    memory: 1500Mi

autoscaling:
  enabled: true
  minReplicas:
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80

global:
  debug: false
  github:
    username:
    token:
  auth:
    secret:
  postgres:
    connection:

github:
  username:
  token:

auth:
  secret:

postgres:
  connection:
