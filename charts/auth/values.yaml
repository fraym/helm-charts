image: ghcr.io/fraym/auth
version: 0.15.1
debug: false
replicas: 2

secret:
initialPasswordSecret:

resources:
  requests:
    cpu: 50m
    memory: 100Mi
  limits:
    memory: 500Mi

autoscaling:
  enabled: true
  minReplicas:
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80

global:
  debug: false
  github:
    username:
    token:
  auth:
    secret:

github:
  username:
  token:

