image: ghcr.io/fraym/auth
version: 0.15.1
debug: false
replicas: 2

secret:
initialPasswordSecret:

resources:
  requests:
    cpu: 50m
    memory: 100Mi
  limits:
    memory: 500Mi

autoscaling:
  enabled: true
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
